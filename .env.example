# Your base domain name for the service
DOMAIN_NAME="your-domain.com"

# Email address for Let's Encrypt certificate registration
EMAIL_ADDRESS="xxxx@xxxx.com"

# Timezone setting. If set to Asia/Shanghai, it will use a domestic mirror for acceleration.
TZ="Asia/Shanghai"

# MongoDB connection settings
MONGODB_USERNAME="root"
MONGODB_PASSWORD="hyacpassword"

# MinIO object storage settings
MINIO_ACCESS_KEY="minio"
MINIO_SECRET_KEY="miniosecret"

# Secret key for JWT token signing and other cryptographic operations
SECRET_KEY="VLDzbSDHLDwpQOFdOKslspVexkfzbEFMskMvLkReERfhHRqxbAPdClKVNsxmRrGkhHNIh"

# Default admin user credentials
DEFAULT_ADMIN_USER="admin"
DEFAULT_ADMIN_PASSWORD="admin123"

# only use for dev mode
DEV_MODE=false
LOCAL_DEV=false # Only used in dev mode
LOCAL_DEV_APP_IMAGE="hyac_app:latest" # Only used in dev mode + local dev
APP_CODE_PATH_ON_HOST="/"  # Path to the app code on the host machine, only used in dev mode

# Build web console in uploader container
# Set to true if you want to build the web console in the uploader container.
WEB_BUILD=false


# Image tag settings for server, web, and app services.
# If these variables are not set, they will default to 'latest'.
# You can use a global tag for all services, or specify a tag for each service individually.

# Example for using a global tag:
# GLOBAL_TAG=v1.0.0
# SERVER_IMAGE_TAG=${GLOBAL_TAG}
# WEB_IMAGE_TAG=${GLOBAL_TAG}
# APP_IMAGE_TAG=${GLOBAL_TAG}

# Example for specifying individual tags:
SERVER_IMAGE_TAG=latest
WEB_IMAGE_TAG=latest
APP_IMAGE_TAG=latest
